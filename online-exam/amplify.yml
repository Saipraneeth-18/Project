version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build/v1   # 👈 where your build output is
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
