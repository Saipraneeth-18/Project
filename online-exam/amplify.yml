version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd cover && npm install
    build:
      commands:
        - cd cover && npm run build
  artifacts:
    baseDirectory: cover/build
    files:
      - '**/*'
  cache:
    paths:
      - cover/node_modules/**/*
