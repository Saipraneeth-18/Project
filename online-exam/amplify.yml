version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
        - cd frontend-directory && npm install  # if needed
    build:
      commands:
        - npm run build  # or specific build command for your frontend
  artifacts:
    baseDirectory: build  # or wherever your build outputs
    files:
      - '**/*'
